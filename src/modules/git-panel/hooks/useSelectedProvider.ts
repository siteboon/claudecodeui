/**
 * The git panel's own name for the shared reader, kept so its imports (and the
 * test that covers a provider switch reaching this panel) stay where they are.
 */
export { useSelectedProvider } from '@/shared/hooks/useSelectedProvider';
