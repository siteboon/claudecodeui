# Claude Code UI Environment Configuration
# Only includes variables that are actually used in the code

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (Express API + WebSocket server)
#API server
PORT=3001
#Frontend port
VITE_PORT=5173

# =============================================================================
# AUTHENTIK OIDC CONFIGURATION
# =============================================================================

# Authentik instance URL (without trailing slash)
AUTHENTIK_URL=https://your-authentik-domain.com

# OAuth2/OIDC Client credentials from Authentik
AUTHENTIK_CLIENT_ID=your-client-id
AUTHENTIK_CLIENT_SECRET=your-client-secret

# Redirect URI for OAuth callback (must match Authentik configuration)
AUTHENTIK_REDIRECT_URI=http://localhost:3001/auth/callback

# Authentik application slug (must match the slug in Authentik)
AUTHENTIK_APP_SLUG=claude-code-ui

# =============================================================================
# SESSION CONFIGURATION
# =============================================================================

# Session secret for Express sessions (use a strong random string)
SESSION_SECRET=your-session-secret-here

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

# Allowed CORS origins in production (comma-separated)
# Example: https://app1.example.com,https://app2.example.com
ALLOWED_ORIGINS=

# =============================================================================
# REDIS CONFIGURATION (Optional)
# =============================================================================

# Redis URL for session storage (optional, defaults to memory store if not set)
# Format: redis://[username:password@]host[:port][/database]
REDIS_URL=redis://localhost:6379