# Claude Code UI Environment Configuration
# This file lists the environment variables used by the application.
# Copy this file to .env and fill in the values for your setup.

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Backend server port (Express API + WebSocket server)
PORT=3001

# Frontend port (for development)
VITE_PORT=5173

# =============================================================================
# SECURITY CONFIGURATION 
# =============================================================================

# JWT Secret for session management (MUST change in production).
# Use a strong random string (minimum 32 characters).
# Example command to generate one: openssl rand -base64 32
# JWT_SECRET=

# Optional API key for additional security (uncomment to enable).
# Use a strong random string for API protection.
# Example: openssl rand -hex 32
# API_KEY=

# =============================================================================
# AI SERVICES
# =============================================================================

# OpenAI API key for voice transcription (required for mic button feature).
# Treat this key like a password and do not commit it to version control.
# OPENAI_API_KEY=

# =============================================================================
# INITIAL SETUP
# =============================================================================

# Create default admin user on first startup (recommended).
# Use strong credentials.
# DEFAULT_USERNAME=admin
# DEFAULT_PASSWORD=

# =============================================================================
# ADVANCED CONFIGURATION (Use with caution)
# =============================================================================

# [Deprecated] Overrides the default projects directory.
# The default path (~/.claude/projects) is recommended.
# PROJECTS_PATH=